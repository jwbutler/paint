type Dimensions = {
  width: number,
  height: number
};

type Coordinates = {
  x: number,
  y: number
};

type Tool = 'draw' | 'line' | 'fill';

export type {
  Coordinates,
  Dimensions,
  Tool
};
